# PRD 详细设计(按页面)

- 生成时间: `2026-03-28 16:01:41`
- 代码识别策略: `mtime-72h`
- 分析文件数量: `18`

## 本次纳入分析的新增/变更文件
- `pages/customer-list.js`
- `pages/customer-list.html`
- `pages/customer-detail.js`
- `assets/js/store.js`
- `pages/customer-detail.html`
- `pages/customer-list.css`
- `assets/css/common.css`
- `pages/customer-detail.css`
- `pages/followup-history.html`
- `pages/followup-detail.html`
- `pages/new-followup.html`
- `pages/followup-history.js`
- `pages/followup-history.css`
- `pages/followup-detail.js`
- `index.html`
- `pages/followup-detail.css`
- `pages/new-followup.js`
- `pages/new-followup.css`

## 页面设计清单
- `customer-detail`
- `customer-list`
- `followup-detail`
- `followup-history`
- `new-followup`

## 页面: 客户详情 - 跳转中

### 变更代码范围
- `pages/customer-detail.html`
- `pages/customer-detail.js`
- `pages/customer-detail.css`
- 共享依赖:
  - `assets/js/store.js`
  - `assets/css/common.css`
  - `index.html`

### 功能点详细设计
1. 触发机制: `click: node -> function`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
2. 触发机制: `click: doneNode -> function`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
3. 触发机制: `tent: 埋点日志：`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
4. 触发机制: `tent: 共 0 条`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
5. 触发机制: `tent: 共 `
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
6. 触发机制: `tent: 请选择跟进类型`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
7. 触发机制: `tent: 请输入自定义标签名称`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
8. 触发机制: `tent: 请填写跟进内容`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
9. 触发机制: `tent: 请选择跟进时间`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
10. 触发机制: `tent: 当前展示：下次待跟进事项（下次跟进时间晚于当前时间）`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。

### 交互流程
1. 用户进入页面，系统初始化基础数据与默认展示状态。
4. 系统在本地状态层完成计算并刷新展示。
5. 页面根据结果进入成功态、失败态或空态。

### 边界情况
- 存在空值/未填写处理逻辑，需定义字段必填与空状态提示。
- 存在必填校验逻辑，需明确逐字段校验时机与提示文案。
- 存在长度或上限控制，需明确边界值与超限提示。
- 存在下限控制，需明确边界值与低于下限时的行为。
- 存在异常处理分支，需定义接口失败、超时、离线时的兜底提示。

### 开发实现要点
- 明确每个交互动作对应的状态机: 初始态 -> 处理中 -> 成功/失败。
- 保证按钮防重复提交与接口超时兜底。
- 对关键输入字段给出完整校验规则和提示文案。

### 页面截图
> [截图占位] 建议放置以下图片:
> 1) 页面首屏(默认态)
> 2) 核心操作后的结果态(如提交成功/筛选结果)
> 3) 失败或空态(如接口错误/无数据)
> 备注: 自动截图失败原因: [WinError 2] 系统找不到指定的文件。

## 页面: 客户列表 - CRM 原型

### 变更代码范围
- `pages/customer-list.html`
- `pages/customer-list.js`
- `pages/customer-list.css`
- 共享依赖:
  - `assets/js/store.js`
  - `assets/css/common.css`
  - `index.html`

### 功能点详细设计
1. 触发机制: `click: btn -> function`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
2. 触发机制: `click: node -> function`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
3. 触发机制: `click: doneNode -> function`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
4. 触发机制: `tent: 埋点日志：`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
5. 触发机制: `tent: 请选择跟进类型`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
6. 触发机制: `tent: 请输入自定义标签名称`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
7. 触发机制: `tent: 请选择跟进时间`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
8. 触发机制: `tent: 共 0 条`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
9. 触发机制: `tent: 共 `
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
10. 触发机制: `tent: 请填写跟进内容`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。

### 交互流程
1. 用户进入页面，系统初始化基础数据与默认展示状态。
2. 用户可录入/筛选字段:
   - `输入客户名称关键词`
   - `typeLabelCustom`
   - `followupTime`
   - `person`
   - `nextFollowupTime`
   - `historyStartDate`
   - `historyEndDate`
3. 用户可执行按钮操作:
   - `重置 Mock 数据`
   - `埋点日志：关`
   - `原型说明`
   - `进入客户详情演示`
   - `重置`
   - `×`
   - `新增跟进`
   - `跟进历史`
4. 系统在本地状态层完成计算并刷新展示。
5. 页面根据结果进入成功态、失败态或空态。

### 边界情况
- 存在空值/未填写处理逻辑，需定义字段必填与空状态提示。
- 存在必填校验逻辑，需明确逐字段校验时机与提示文案。
- 存在长度或上限控制，需明确边界值与超限提示。
- 存在下限控制，需明确边界值与低于下限时的行为。
- 存在异常处理分支，需定义接口失败、超时、离线时的兜底提示。
- 存在禁用态逻辑，需定义按钮/输入框在不可操作时的触发条件。
- 存在确认操作，需定义二次确认弹窗文案与撤销路径。

### 开发实现要点
- 明确每个交互动作对应的状态机: 初始态 -> 处理中 -> 成功/失败。
- 保证按钮防重复提交与接口超时兜底。
- 对关键输入字段给出完整校验规则和提示文案。

### 页面截图
> [截图占位] 建议放置以下图片:
> 1) 页面首屏(默认态)
> 2) 核心操作后的结果态(如提交成功/筛选结果)
> 3) 失败或空态(如接口错误/无数据)
> 备注: 自动截图失败原因: [WinError 2] 系统找不到指定的文件。

## 页面: 跟进记录详情 - CRM 原型

### 变更代码范围
- `pages/followup-detail.html`
- `pages/followup-detail.js`
- `pages/followup-detail.css`
- 共享依赖:
  - `assets/js/store.js`
  - `assets/css/common.css`
  - `index.html`

### 功能点详细设计
1. 触发机制: `change: typeEl -> function`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
2. 触发机制: `tent: 请选择跟进类型`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
3. 触发机制: `tent: 请输入自定义标签名称`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
4. 触发机制: `tent: 请填写跟进内容`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。

### 交互流程
1. 用户进入页面，系统初始化基础数据与默认展示状态。
3. 用户可执行按钮操作:
   - `原型说明`
   - `取消`
   - `确认删除`
   - `我知道了`
4. 系统在本地状态层完成计算并刷新展示。
5. 页面根据结果进入成功态、失败态或空态。

### 边界情况
- 存在空值/未填写处理逻辑，需定义字段必填与空状态提示。
- 存在必填校验逻辑，需明确逐字段校验时机与提示文案。
- 存在长度或上限控制，需明确边界值与超限提示。
- 存在下限控制，需明确边界值与低于下限时的行为。
- 存在异常处理分支，需定义接口失败、超时、离线时的兜底提示。
- 存在确认操作，需定义二次确认弹窗文案与撤销路径。

### 开发实现要点
- 明确每个交互动作对应的状态机: 初始态 -> 处理中 -> 成功/失败。
- 保证按钮防重复提交与接口超时兜底。
- 对关键输入字段给出完整校验规则和提示文案。

### 页面截图
> [截图占位] 建议放置以下图片:
> 1) 页面首屏(默认态)
> 2) 核心操作后的结果态(如提交成功/筛选结果)
> 3) 失败或空态(如接口错误/无数据)
> 备注: 自动截图失败原因: [WinError 2] 系统找不到指定的文件。

## 页面: 跟进历史 - 跳转中

### 变更代码范围
- `pages/followup-history.html`
- `pages/followup-history.js`
- `pages/followup-history.css`
- 共享依赖:
  - `assets/js/store.js`
  - `assets/css/common.css`
  - `index.html`

### 功能点详细设计
1. 触发机制: `click: node -> function`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
2. 触发机制: `tent: 埋点日志：`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
3. 触发机制: `tent: 请选择跟进类型`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
4. 触发机制: `tent: 请输入自定义标签名称`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
5. 触发机制: `tent: 请选择跟进时间`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
6. 触发机制: `tent: 请填写跟进内容`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。

### 交互流程
1. 用户进入页面，系统初始化基础数据与默认展示状态。
4. 系统在本地状态层完成计算并刷新展示。
5. 页面根据结果进入成功态、失败态或空态。

### 边界情况
- 存在空值/未填写处理逻辑，需定义字段必填与空状态提示。
- 存在必填校验逻辑，需明确逐字段校验时机与提示文案。
- 存在长度或上限控制，需明确边界值与超限提示。
- 存在下限控制，需明确边界值与低于下限时的行为。
- 存在异常处理分支，需定义接口失败、超时、离线时的兜底提示。
- 存在确认操作，需定义二次确认弹窗文案与撤销路径。

### 开发实现要点
- 明确每个交互动作对应的状态机: 初始态 -> 处理中 -> 成功/失败。
- 保证按钮防重复提交与接口超时兜底。
- 对关键输入字段给出完整校验规则和提示文案。

### 页面截图
> [截图占位] 建议放置以下图片:
> 1) 页面首屏(默认态)
> 2) 核心操作后的结果态(如提交成功/筛选结果)
> 3) 失败或空态(如接口错误/无数据)
> 备注: 自动截图失败原因: [WinError 2] 系统找不到指定的文件。

## 页面: 新增跟进记录 - CRM 原型

### 变更代码范围
- `pages/new-followup.html`
- `pages/new-followup.js`
- `pages/new-followup.css`
- 共享依赖:
  - `assets/js/store.js`
  - `assets/css/common.css`
  - `index.html`

### 功能点详细设计
1. 触发机制: `change: fields.typeKey -> function`
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。
2. 触发机制: `tent: 已填写 `
   - 入口交互: 用户在当前页面触发上述事件。
   - 系统行为: 执行对应处理函数，更新页面状态与数据。
   - 结果反馈: 在页面上显示成功、失败或空态反馈。

### 交互流程
1. 用户进入页面，系统初始化基础数据与默认展示状态。
2. 用户可录入/筛选字段:
   - `例如：法务沟通 / 合同推进`
   - `followupTime`
   - `person`
   - `nextFollowupTime`
   - `请输入本次跟进内容，建议描述客户反馈、问题及下一步动作。`
3. 用户可执行按钮操作:
   - `原型说明`
   - `提交并保存`
   - `我知道了`
4. 系统在本地状态层完成计算并刷新展示。
5. 页面根据结果进入成功态、失败态或空态。

### 边界情况
- 存在必填校验逻辑，需明确逐字段校验时机与提示文案。
- 存在长度或上限控制，需明确边界值与超限提示。
- 存在异常处理分支，需定义接口失败、超时、离线时的兜底提示。

### 开发实现要点
- 明确每个交互动作对应的状态机: 初始态 -> 处理中 -> 成功/失败。
- 保证按钮防重复提交与接口超时兜底。
- 对关键输入字段给出完整校验规则和提示文案。

### 页面截图
> [截图占位] 建议放置以下图片:
> 1) 页面首屏(默认态)
> 2) 核心操作后的结果态(如提交成功/筛选结果)
> 3) 失败或空态(如接口错误/无数据)
> 备注: 自动截图失败原因: [WinError 2] 系统找不到指定的文件。
