# 题目一  CRM 客户跟进事项原型（项目说明）

## Vibe Coding快速实现原型核心思路

1. 根据题目要求，快速从用户角度拆解需求
2. 根据需求，编写PRD、明确背景与目标、核心体验路径、概要设计（模块、功能清单、页面关系图、推荐埋点等）
3. 上述PRD喂给AI明确原型的实现范围
4. 组合Prompt完成初版设计

——以上耗时30分钟——

   5.不断验收迭代，体验并优化到可接受范围

——以上耗时40分钟——

​    6.根据实现的原型编写完成PRD的详细设计部分

——以上耗时1小时（后续可编写SKILL沉淀PRD技能加快此速度）——

## 1. 项目定位
本项目是围绕「客户跟进事项」需求完成的前端交互原型，核心目标是：
- 在客户列表与客户详情链路中，支持持续录入跟进事件。
- 支持按时间与类型追溯历史记录。
- 支持编辑、删除、筛选、校验与埋点，保证可演示与可复盘。

本原型使用原生 `HTML + CSS + JavaScript` 实现，全部为前端 mock 数据，不依赖后端接口。

## 2. 如何运行
1. 打开项目根目录下 `index.html`（会自动进入客户列表页）。
2. 推荐使用浏览器直接打开，或使用本地静态服务打开均可。
3. 所有交互均可在前端直接演示（无需启动后端）。

## 3. 建议评审路径
1. 进入客户列表页，查看每个客户的最近跟进摘要、最近跟进时间、快捷操作。
2. 在列表页点击「查看详情」，打开客户详情弹窗。
3. 在客户详情的跟进模块点击「新增跟进」，验证表单校验、提交成功、列表与摘要同步。
4. 点击「跟进历史」，验证：
   - 类型筛选
   - 日期筛选
   - 日期清空（输入框右侧 x）
   - 重置筛选
   - 滚动自动加载
5. 点击任意历史卡片进入跟进详情，验证：
   - 编辑（含跟进时间可编辑）
   - 删除二次确认
   - 下次跟进“已跟进/撤销已跟进”可逆
   - 数据联动（待跟进计数自动变化）
6. 返回列表，点击「重置 mock 数据」，验证全局恢复初始状态。
7. 点击左侧导航栏的管理者视图，完成浏览管理者视图的前瞻功能

## 5. 功能实现对照（核心）
| 模块 | 已实现内容 | 说明 |
|---|---|---|
| 客户列表 | 客户基础信息、最近跟进摘要、最近跟进时间、查看详情、跟进历史、新增跟进 | 作为业务起点页 |
| 客户详情（弹窗） | 客户基础信息、最近跟进卡、跟进统计卡、新增跟进、跟进历史入口 | 关闭仅影响当前层弹窗 |
| 新增跟进（弹窗） | 跟进类型、跟进时间、跟进人、内容（非必填）、下次跟进时间；实时校验与提交 | 提交后多视图同步 |
| 跟进历史（弹窗） | 时间线卡片、类型筛选、时间筛选、重置筛选、自动滚动加载、总条数显示 | 空状态可引导新增 |
| 跟进详情（弹窗） | 查看完整字段、编辑、删除、已跟进状态切换 | 未设置下次跟进时间时不展示该字段 |
| 埋点与调试 | 关键行为埋点、日志面板开关、事件时间记录 | 跟进内容埋点改为失焦触发（blur） |
| 数据管理 | 本地 mock 数据集中管理、重置按钮恢复初始数据 | 便于评审反复演示 |

## 6.Prompt参考

1. 核心提示Prompt

```
你现在是一名资深 B 端产品原型前端工程师，请根据下面的需求，生成一个“可直接打开运行的单文件 HTML 交互原型”，用于展示 CRM 系统中的“客户跟进事项”功能。

【输出要求】
1. 只输出完整可运行代码，不要解释，不要 markdown 代码块。
2. 产出一个单文件 index.html。
3. 必须使用原生 HTML + CSS + JavaScript 实现，不要依赖 npm、构建工具、外部接口。
4. 所有数据使用前端 mock 数据写死在页面中。
5. 页面要可交互、可演示，不是静态图。
6. 风格参考现代 B 端 SaaS 后台：简洁、专业、信息清晰，接近 CRM / OA / 企业管理后台风格。
7. 所有文案使用中文。
8. 适配桌面端，默认画布宽度以 1440px 左右后台页面为参考。
9. 页面内请适当加入提示说明，让面试/汇报时容易演示。
10. 所有弹窗、抽屉、表单校验、筛选、切换、hover、空状态、成功提示都要可操作。

【项目背景】
这是一个 CRM 模块中的“客户跟进事项”功能原型。目标是在客户详情页中新增“跟进记录”能力，用于记录销售与客户之间的触达行为，例如电话沟通、线上会议、上门拜访等，并支持历史追溯、筛选查看、编辑删除。核心目的是让客户跟进过程结构化沉淀，支撑后续 KPI、销售管理和数据分析。

【你要实现的原型范围】
--根据《提供的PRD》完成抽取分析--
```

